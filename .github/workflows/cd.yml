name: CD - Deploy to EC2

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test CD Trigger
        run: |
          echo "============================================"
          echo "✅ CD workflow 成功被 GitHub 載入並執行！"
          echo "時間: $(date)"
          echo "Commit SHA: ${{ github.sha }}"
          echo "============================================"